/// <reference types="react" />
import { SliderProps } from './types';
import '../UI/styles.css';
declare const Slider: ({ slidesNumber, spaceBetweenSlides, nextButton, prevButton, children, config, showDots, customDot, customActiveDot, dotColor, activeDotColor, sizeForDefaultDot, sizeForDefaultActiveDot, autoplay, autoplaySpeed, dotsAnimation, animationSpeed, slidesAnimation, className }: SliderProps) => JSX.Element;
export default Slider;
