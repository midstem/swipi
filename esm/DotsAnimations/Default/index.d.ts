/// <reference types="react" />
import { DotsTypes } from '../../types';
declare const Default: ({ children, slideIndex, customDot, customActiveDot, sizeForDefaultDot, sizeForDefaultActiveDot, dotColor, activeDotColor, handleDotClick, returnDots }: DotsTypes) => JSX.Element;
export default Default;
