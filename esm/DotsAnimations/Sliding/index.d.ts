/// <reference types="react" />
import { DotsTypes } from '../../types';
declare const Sliding: ({ children, customDot, slideIndex, customActiveDot, dotColor, sizeForDefaultDot, sizeForDefaultActiveDot, activeDotColor, animationSpeed, handleDotClick }: DotsTypes) => JSX.Element;
export default Sliding;
