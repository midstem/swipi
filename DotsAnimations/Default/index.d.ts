import { DotsTypes } from '../../types';
declare const Default: ({ slideIndex, customDot, customActiveDot, sizeForDefaultDot, sizeForDefaultActiveDot, dotColor, activeDotColor, handleDotClick, returnDots, countShowDots }: DotsTypes) => JSX.Element;
export default Default;
