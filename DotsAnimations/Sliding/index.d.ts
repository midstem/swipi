import { DotsTypes } from '../../types';
declare const Sliding: ({ customDot, slideIndex, customActiveDot, dotColor, sizeForDefaultDot, sizeForDefaultActiveDot, activeDotColor, animationSpeed, handleDotClick, countShowDots }: DotsTypes) => JSX.Element;
export default Sliding;
