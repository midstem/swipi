# @swipi/playground

A local playground environment for testing the Swipi library.

## Getting Started

To run the playground locally, you can use either of the following commands from the root of the monorepo:

```bash
npm run start --workspace @swipi/playground
```

Or, if you are inside the `apps/playground` directory:

```bash
npm run start
```
